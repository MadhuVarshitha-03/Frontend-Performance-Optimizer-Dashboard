Frontend Performance Optimizer Dashboard
HTML · CSS · JavaScript

A visual dashboard that analyzes frontend performance by evaluating DOM size, load time, repaint cost, and CSS rule weight. It produces a dynamic performance score and provides optimization suggestions based on real browser metrics.

This project simulates real-world UI performance diagnostics and teaches practical frontend efficiency concepts.

🚀 Features

✔ DOM element count analysis
✔ Page load time measurement
✔ Synthetic repaint/render test
✔ CSS rule + inline style tracking
✔ Automatic scoring system (0–100)
✔ Performance status indicators
✔ Suggestions + reasoning engine
✔ Full timeline logs
✔ Self-contained — no libraries
✔ Responsive UI

📊 Performance Metrics Used
Metric	Description
DOM Size	Total number of elements in the document
Load Time	Performance API timing to load event end
Repaint Cost	Synthetic toggling layout + repaint stress test
CSS Structure	StyleSheets + rule counts + inline styling
🎯 Tech Stack
Technology	Purpose
HTML	UI Layout + Structure
CSS	Styling + Dark theme design
JavaScript	Performance analysis logic
